apiVersion: v1
kind: Service
metadata:
  name: nginx-b-service
  labels: { app: nginx-b }
spec:
  type: ClusterIP
  selector: { app: nginx-b }
  ports:
    - name: http
      port: 80
      targetPort: 80
