apiVersion: v1
kind: Service
metadata:
  name: wordpress-b-service
  labels: { app: wordpress-b }
spec:
  selector: { app: wordpress-b }
  ports:
    - name: http
      port: 80
      targetPort: http
